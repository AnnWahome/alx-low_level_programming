#include "main.h"
/**
 * add - function that adds two integers together
 * @num1: takes in an integer
 * @num2: takes in an integer
 * Return: a + b result
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
